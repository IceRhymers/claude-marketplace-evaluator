---
name: deploy-service
description: Deploy the application to a target environment
---

Run the deployment pipeline for a specified environment (staging, production). Verifies the build, executes deployment scripts, and checks health endpoints.
